export namespace compilers {
    namespace solc {
        const version: string;
        namespace settings {
            namespace optimizer {
                const enabled: boolean;
                const runs: number;
            }
            const evmVersion: string;
        }
    }
}
export namespace networks {
    namespace development {
        const host: string;
        const network_id: string;
        const port: number;
    }
}
//# sourceMappingURL=truffle-config.d.ts.map